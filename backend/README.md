## Requirements

+ [uv](https://github.com/astral-sh/uv)

## Setup

1. Open terminal in `backend`
2. Run `uv sync`
3. Start the server with `uv run python manage.py runserver`
4. Access the development server on `http://127.0.0.1:8000/`
