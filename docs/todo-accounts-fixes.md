Fixes for the future. Ignore for now.

- proper names for members instead of just emails that can be edited by the member
- link back to the member page by clicking on the name in the top right

- OTP timeout is too harsh. give more leniency. reset the timer for a member on successful login or log out
- log out should have a confirmation. it should also take you to a log out page that can redirect you to the home page with a button
- auth tokens don't seem to work, trying to access the member page again results in a new login prompt.

- redo join page
- redo member page
- both need smooth transitions

- make the join button and member profile button more flashy
- replace reach out in some areas with join.